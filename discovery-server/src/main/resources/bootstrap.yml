spring:
  application:
    name: eureka-config
  cloud:
    config:
      uri: http://localhost:28084
      username: root
      password: root

#eureka:
#  client:
    #register-with-eureka: true
    #fetch-registry: false

#server:
  #port: 8761